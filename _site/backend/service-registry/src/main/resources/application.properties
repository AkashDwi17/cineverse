spring.application.name=service-registry
server.port=8761
eureka.client.register-with-eureka=false

#by default service registry acts like client ,we should avoid it and usually without service registry is not there means then ms1 comunicatio needs to be done with ms2 and for we ms1 should know host name and port name of ms2,to overcome this service registry comes into picture where ms1 should ms2 name thats enought to communicate
eureka.client.fetch-registry=false
